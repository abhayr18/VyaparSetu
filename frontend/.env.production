# Production/packaged build: the Express server serves this SPA from its own
# origin, so API calls are relative (/api/...). An empty value here overrides the
# dev .env (http://localhost:5000) and, via the ?? in apiService.js, becomes "".
VITE_API_BASE_URL=
